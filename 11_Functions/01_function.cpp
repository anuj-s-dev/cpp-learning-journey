/*Functions:- It is a block of code that we use many times when needed. Whe we need this:- It tackel repetition. There are certain piece of code that we use a lot of times in problem */

// We made new function above main function always.
 
#include<iostream>
using namespace std;
int main()  // This is our main function. 
{
    cout<<"Good Morning"<<endl;
    cout<<"How are you?"<<endl;
}


// Normal way of writing. But when we make new function it look like see in next file. 

// In loop when we write anything in between loop then loop was not working it stop, but in function we write anything in between the function it can not stop.

// By using function we use one block of code many time by calling the function name again and again. Doesn't matter anything was wriiten in between or not. 