#include <iostream>
using namespace std;
int main()
{

    // int = Integer

    int x = 5; /* Here int = integer and x = variable. When we want to store integer then we use int data type it store only integer.
    
    if we add , sub, multiply, or divide int with int we got only int not float or other data type.
    */
    cout << x;
}